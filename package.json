{
  "name": "intellicpp",
  "version": "2.0.0",
  "description": "IntelliCPP frontend and backend orchestration",
  "engines": {
    "node": ">=16.0.0"
  },
  "scripts": {
    "dev": "npx concurrently -n \"BACKEND,FRONTEND\" -c \"bgBlue.bold,bgMagenta.bold\" \"npm run dev:server\" \"npm run dev:frontend\"",
    "dev:frontend": "cd frontend && npm start",
    "dev:server": "cd backend && (npx nodemon server.js || node server.js)",
    "build:frontend": "cd frontend && npm run build",
    "install-deps": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "dependencies": {
    "concurrently": "^8.2.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
